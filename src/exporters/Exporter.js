/**
 * @author James Edwards <foobarprotocol@librehash.com>
 */

import { Volume } from "../volume";

/**
 * Class for exporting voxel data.
 * @memberof module:voxelizer/exporters
 */
export class Exporter {

  /**
   * Export the volume data.
   * @param {Volume} voxels 3D array with Voxels.
	 * @param {function()} [onDone] Callback with exported data.
   */
  parse(volume, onDone) { }

}
